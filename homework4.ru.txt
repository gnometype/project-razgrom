g=int(input('Введите глобальную переменную'))
def f1():
    n=int(input('Введите охватывающую переменную'))
    def f2(x=int(input('Введите параметр функции'))):
        l=int(input('Введите локальную переменную'))
        global g
        nonlocal n
        print('сумма всех переменных',g+n+l+x)
    f2()
f1()
